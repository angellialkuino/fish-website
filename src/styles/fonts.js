import { Wendy_One, Commissioner } from "next/font/google";

export const wendy_one = Wendy_One({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-wendy-one",
});

export const commissioner = Commissioner({
  weight: ["400", "700"],
  subsets: ["latin"],
  variable: "--font-commissioner",
});
